{
  "name": "prisma-decimal",
  "version": "1.1.0",
  "description": "Decimal convertor of PrismaClient",
  "main": "index.ts",
  "scripts": {
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jestconfig.json",
    "prepare" : "npm run build",
    "prepublishOnly" : "npm test && npm run lint",
    "preversion" : "npm run lint",
    "version" : "npm run format && git add -A src",
    "postversion" : "git push && git push --tags",
    "build": "tsc"
  },
  "keywords": [
    "prisma",
    "decimal"
  ],
   "repository": {
    "type": "git",
    "url": "https://github.com/developerkitchentr/prisma-decimal"
  },
  "author": "Uygun Bodur @DevKit",
  "license": "MIT",
  "devDependencies": {
     "@types/jest": "^29.2.3",
    "jest": "^29.3.1",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "4.10.1",
    "prisma": "^4.10.1",
    "@nestjs/common": "^9.0.0"
  },
  "files": [
    "lib/**/*"
  ]
}
